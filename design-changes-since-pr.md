# Design changes since the initial PR push (temp, for admin use — do not commit)

Baseline: the client PR state first pushed to origin on **2026-07-23** (commits `3f5f109` 11:25, `0a397c7` 12:05). Everything below happened after that push, verifiable in `git log` on branch `initial-version`. Decision IDs (Q17–Q22) reference the design-decision ledger.

| Date | Work | Decision IDs | Landed in commit |
|---|---|---|---|
| 2026-07-23 (afternoon) | GraalVM native-image support: full reflect/proxy/resource configs for the Azure SDK stack, tracing-agent capture + trim methodology, native workflow added; suite verified 52/52 under `bal test --graalvm` | — | `95929c0` (16:48) |
| 2026-07-23 | Owner-decided API corrections from the skill audits: `RETRY_POLICY_TYPE`/`PROXY_TYPE` enums renamed to PascalCase; lease error mapping made status-faithful (`LeaseIdMissing` + 3 lease data-op codes → `PreconditionFailedError`) | Q18 | `95929c0` / `c584a48` |
| 2026-07-24 (morning) | CodeRabbit round (13 comments adjudicated): 4 dismissed with ecosystem-parity evidence; fixes: AfterSuite cleanup made best-effort + run-scoped prefix, symlink percent-decoding bug (real connector bug, `+`-preservation), examples switched to least-privilege file-scoped SAS, spec gained the five Entra ID record definitions, tests README corrected and tightened (~40% cut, evidence-based) | Q19 | `c584a48` |
| 2026-07-24 (midday) | Design-team review round 1 (ThisaruGuruge): concurrency note dropped from class docs, `config` param wording, license headers added to build files, repo-wide doc-comment brevity sweep across the API surface (low-code-view standard) | Q20 | `ae67c9d` (12:20) |
| 2026-07-24 (13:42) | Entra `kind` discriminators made required (defaulted `kind` proven ambiguous against the union empirically); spec Config.toml examples completed for all five Entra records | Q21 | `a09e5ed` |
| 2026-07-24 (afternoon) | Design-team review round 2 (Nuvindu, 12 comments): license headers normalized to canonical 2026 WSO2 form repo-wide; `var` eliminated; release version set to **1.0.0**; README `## Overview` + configurable-based quickstart; magic enum-value strings replaced with constants; XML/JSON value round-trip test added; TLS trust-config ifs flattened; `.gitattributes` eol rules; `configs.bal` split defended with rationale | Q22 | `c584a48` (16:32) |
| 2026-07-24 (evening) | Constants architecture settled with reviewer: central `Constants.java` (190 constants) fully dissolved into owner classes (RecordMapper/OptionsReader/Ops et al.), fixing a latent `path` field conflation; backed by a cluster-level usage analysis and an ecosystem survey (mongodb vs ftp/http generations) | Q22 (amended) | `c584a48` (16:32) |

Supporting detail for every row lives in the decision ledger (Q17–Q22 entries carry the full rationale and dates) and in the per-commit diffs. The unified dual-mode test suite (Q17, 2026-07-22) and PR-readiness infrastructure predate the baseline push and are in `3f5f109`/`0a397c7` themselves.
